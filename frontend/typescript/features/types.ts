let date1 = new Date() // Inferred type (implicit)
let date2: Date = new Date() // Explicit Type

const person = {
  age: 26,
}

class Color {}

const red = new Color()

// Hover over the variables to see how the data type of a value shows up from the TS Compiler in our editor

console.log(person)
